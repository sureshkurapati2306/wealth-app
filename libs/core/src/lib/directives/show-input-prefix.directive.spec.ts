import { ShowInputPrefixDirective } from './show-input-prefix.directive';

describe('ShowInputPrefixDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowInputPrefixDirective();
    expect(directive).toBeTruthy();
  });
});
